<Window x:Class="flapping_penguin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:flapping_penguin"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="40" Width="200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- ここに x:Name="CatImage" があることで、C#側から操作できるようになります -->
            <Image x:Name="CatImage" Width="300" Height="300" />

            <Button Content="テスト" Width="200" Height="50" Margin="0,20,0,0" Click="TestButton_Click" />
        </StackPanel>
    </Grid>
</Window>
